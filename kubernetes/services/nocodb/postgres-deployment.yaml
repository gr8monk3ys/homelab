---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nocodb-postgres
  namespace: nocodb
  labels:
    app: nocodb-postgres
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: nocodb-postgres
  template:
    metadata:
      labels:
        app: nocodb-postgres
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
      - name: postgres
        image: postgres:15.10
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        env:
        - name: POSTGRES_USER
          value: "nocodb"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: nocodb-db-password
              key: password
        - name: POSTGRES_DB
          value: "nocodb"
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - nocodb
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - nocodb
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: nocodb-postgres-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nocodb-postgres-pvc
  namespace: nocodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
---
apiVersion: v1
kind: Service
metadata:
  name: nocodb-postgres
  namespace: nocodb
spec:
  selector:
    app: nocodb-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: nocodb-db-password
  namespace: nocodb
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: homelab-secret-store
    kind: ClusterSecretStore
  target:
    name: nocodb-db-password
    creationPolicy: Owner
  data:
  - secretKey: password
    remoteRef:
      key: nocodb-db-password
      property: password
