---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: open-webui
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 2Gi
    limits.cpu: "2"
    limits.memory: 4Gi
    persistentvolumeclaims: "3"
    requests.storage: 20Gi
    pods: "5"
