---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: yarr
spec:
  hard:
    requests.cpu: "200m"
    requests.memory: 256Mi
    limits.cpu: "500m"
    limits.memory: 512Mi
    persistentvolumeclaims: "2"
    requests.storage: 5Gi
    pods: "3"
