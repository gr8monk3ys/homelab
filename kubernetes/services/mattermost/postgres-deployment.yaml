---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mattermost-postgres
  namespace: mattermost
  labels:
    app: mattermost-postgres
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mattermost-postgres
  template:
    metadata:
      labels:
        app: mattermost-postgres
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
      - name: postgres
        image: postgres:15.10
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        env:
        - name: POSTGRES_USER
          value: "mattermost"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mattermost-db-password
              key: password
        - name: POSTGRES_DB
          value: "mattermost"
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - mattermost
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - mattermost
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: mattermost-postgres-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mattermost-postgres-pvc
  namespace: mattermost
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
---
apiVersion: v1
kind: Service
metadata:
  name: mattermost-postgres
  namespace: mattermost
spec:
  selector:
    app: mattermost-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mattermost-db-password
  namespace: mattermost
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: homelab-secret-store
    kind: ClusterSecretStore
  target:
    name: mattermost-db-password
    creationPolicy: Owner
  data:
  - secretKey: password
    remoteRef:
      key: mattermost-db-password
      property: password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mattermost-secret
  namespace: mattermost
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: homelab-secret-store
    kind: ClusterSecretStore
  target:
    name: mattermost-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        database-url: "postgres://mattermost:{{ .password }}@mattermost-postgres:5432/mattermost?sslmode=prefer&connect_timeout=10"
  data:
  - secretKey: password
    remoteRef:
      key: mattermost-db-password
      property: password
