---
# Service descriptor: the install interface read by scripts/lib/services.sh.
name: homepage
namespace: homepage
group: core
priority: 90
url: home
description: Dashboard with Kubernetes-aware widgets

# Per-namespace isolation: policy templates under kubernetes/security/network-policies/templates/.
# Deliberately open: cluster-mode widgets need the Kubernetes API and every namespace.
networkPolicies: []
