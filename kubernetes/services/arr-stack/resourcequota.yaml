---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: arr-stack
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    persistentvolumeclaims: "10"
    requests.storage: 100Gi
    pods: "15"
