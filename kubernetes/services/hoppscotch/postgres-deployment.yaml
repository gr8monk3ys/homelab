---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hoppscotch-postgres
  namespace: hoppscotch
  labels:
    app: hoppscotch-postgres
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: hoppscotch-postgres
  template:
    metadata:
      labels:
        app: hoppscotch-postgres
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
      - name: postgres
        image: postgres:15.10
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        env:
        - name: POSTGRES_USER
          value: "hoppscotch"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: hoppscotch-db-password
              key: password
        - name: POSTGRES_DB
          value: "hoppscotch"
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - hoppscotch
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - hoppscotch
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: hoppscotch-postgres-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hoppscotch-postgres-pvc
  namespace: hoppscotch
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
---
apiVersion: v1
kind: Service
metadata:
  name: hoppscotch-postgres
  namespace: hoppscotch
spec:
  selector:
    app: hoppscotch-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: hoppscotch-db-password
  namespace: hoppscotch
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: homelab-secret-store
    kind: ClusterSecretStore
  target:
    name: hoppscotch-db-password
    creationPolicy: Owner
  data:
  - secretKey: password
    remoteRef:
      key: hoppscotch-db-password
      property: password
