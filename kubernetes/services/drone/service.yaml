---
# Service descriptor: the install interface read by scripts/lib/services.sh.
name: drone
namespace: drone
group: dev
url: drone
description: Drone CI (no runner by default)

# Per-namespace isolation: policy templates under kubernetes/security/network-policies/templates/.
# Deliberately open: reaches Gitea through its public hostname (the LoadBalancer IP),
# which no template expresses yet.
networkPolicies: []
