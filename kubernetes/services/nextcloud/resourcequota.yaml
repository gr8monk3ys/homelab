---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: nextcloud
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 2Gi
    limits.cpu: "2"
    limits.memory: 4Gi
    persistentvolumeclaims: "5"
    requests.storage: 200Gi
    pods: "10"
