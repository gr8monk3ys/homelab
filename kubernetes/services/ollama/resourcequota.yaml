---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: ollama
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 16Gi
    limits.cpu: "8"
    limits.memory: 32Gi
    persistentvolumeclaims: "5"
    requests.storage: 100Gi
    pods: "10"
