# Legacy

This directory contains **archived** manifests kept for reference.

Manifests under `legacy/` are not used by `setup-v2.sh` and are intentionally excluded from the main `kubernetes/` tree to avoid accidental use and reduce noise in automated validation/scanning.

