---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: drone
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    persistentvolumeclaims: "5"
    requests.storage: 20Gi
    pods: "10"
